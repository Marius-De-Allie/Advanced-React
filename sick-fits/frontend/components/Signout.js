import React from 'react';
import { Mutation } from 'react-apollo';
import gql from 'graphql-tag';
// TODO add CURRENT_USER_MUTATION when it's created.

const Signout = props => <button>SignOut</button>;

export default Signout;